#include<stdio.h>
void inputArray(int *x, int n);
int getFrequency(int *x, int n, int k);
int getMaxposn(int *x, int n);
int main()
{
	int a[100], f[100], i, n;
	printf("Enter the no. of intergers in array(n)\n");
	scanf("%d", &n);

	input array(int *x, int n, int k);

	for(i=0; i<n; i++)
	{ 
		f[i] = getFrequency(int *x, int n, int k);
	}

	p = getMaxposn(int *x, int n);
	printf("%d\n", &a[p]);
	return 0;
}




